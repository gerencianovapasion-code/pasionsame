═══════════════════════════════════════════════════════════════
🚀 EJECUTA ESTOS COMANDOS EN TU SERVIDOR HOSTINGER
═══════════════════════════════════════════════════════════════

Estás en: [root@srv729558 pasionsame]#

┌─────────────────────────────────────────────────────────────┐
│  PASO 1: VERIFICAR ARCHIVO .env                             │
└─────────────────────────────────────────────────────────────┘

cat .env

✓ Verifica que DATABASE_URL esté correcta
✓ Verifica que NEXTAUTH_SECRET esté configurado
✓ Verifica que NEXTAUTH_URL esté configurado

Si necesitas editarlo:

nano .env

(Guardar: Ctrl+O, Enter, Ctrl+X)


┌─────────────────────────────────────────────────────────────┐
│  PASO 2: EJECUTAR SCRIPT DE DEPLOYMENT                      │
└─────────────────────────────────────────────────────────────┘

bash deploy.sh

El script te preguntará si quieres sincronizar la BD.
Responde: s (sí)

Esto tomará ~5 minutos.


┌─────────────────────────────────────────────────────────────┐
│  PASO 3: VERIFICAR QUE TODO FUNCIONA                        │
└─────────────────────────────────────────────────────────────┘

pm2 status

Deberías ver:
┌─────┬──────────────┬─────────┬─────────┐
│ id  │ name         │ status  │ cpu     │
├─────┼──────────────┼─────────┼─────────┤
│ 0   │ nextjs-app   │ online  │ 0%      │
│ 1   │ socket-server│ online  │ 0%      │
└─────┴──────────────┴─────────┴─────────┘


┌─────────────────────────────────────────────────────────────┐
│  PASO 4: VER LOGS (OPCIONAL)                                │
└─────────────────────────────────────────────────────────────┘

pm2 logs

(Presiona Ctrl+C para salir)


┌─────────────────────────────────────────────────────────────┐
│  PASO 5: CONFIGURAR FIREWALL                                │
└─────────────────────────────────────────────────────────────┘

firewall-cmd --permanent --add-service=http
firewall-cmd --permanent --add-service=https
firewall-cmd --permanent --add-port=3000/tcp
firewall-cmd --permanent --add-port=3001/tcp
firewall-cmd --reload


┌─────────────────────────────────────────────────────────────┐
│  SIGUIENTE: CONFIGURAR DOMINIOS EN CYBERPANEL               │
└─────────────────────────────────────────────────────────────┘

Ver guía completa: GUIA_DEPLOYMENT_HOSTINGER.md
Sección: "FASE 5: CONFIGURAR DOMINIOS"

O seguir: EJECUTAR_EN_SERVIDOR.md


═══════════════════════════════════════════════════════════════
✅ RESUMEN DE LO QUE HARÁ deploy.sh:
═══════════════════════════════════════════════════════════════

1. Instalar dependencias (bun install)
2. Generar cliente Prisma
3. Crear 37 tablas en base de datos ⭐
4. Compilar aplicación (build)
5. Crear directorios necesarios
6. Configurar y arrancar PM2
7. Iniciar Next.js (puerto 3000)
8. Iniciar Socket.io (puerto 3001)


═══════════════════════════════════════════════════════════════
🆘 SI HAY PROBLEMAS:
═══════════════════════════════════════════════════════════════

Error de base de datos:
→ Verificar: cat .env | grep DATABASE_URL
→ Probar: mysql -u infl_pasiones_user -p infl_pasiones_prod

PM2 no encontrado:
→ npm install -g pm2

Build falla:
→ rm -rf .next && bun install && bun run build


═══════════════════════════════════════════════════════════════
📋 DESPUÉS DEL DEPLOYMENT:
═══════════════════════════════════════════════════════════════

1. Configurar dominios en CyberPanel
2. Configurar proxy reverso
3. Instalar SSL
4. Probar sitios en navegador
5. Configurar webhooks de Stripe


═══════════════════════════════════════════════════════════════

🚀 ¡Ejecuta bash deploy.sh para empezar!

═══════════════════════════════════════════════════════════════
